# Synthetic OHR contract fixture

`synthetic-qas.json` is handcrafted test data shaped like the public OHR-Bench
`data/qas_v2.json` contract, including scalar/list evidence, source page index zero,
and an excluded `multi` item. It contains no copied OHR-Bench questions, answers, or PDFs.
